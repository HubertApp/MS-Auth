services:
  service-auth:
    build: .
    container_name: service_auth
    ports:
      - "3004:3000"
    env_file:
      - .env
    networks:
        - hubert-network

volumes:
  mongo_data:

networks:
  hubert-network:
    external: true